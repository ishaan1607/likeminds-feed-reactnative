import { StyleSheet } from "react-native";

// default icon style
export const defaultStyles = StyleSheet.create({
    iconStyle: {
      width: 25,
      height: 25,
      resizeMode: 'contain',
    },
  });