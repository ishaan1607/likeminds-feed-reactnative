import { StyleSheet } from "react-native";

// default text style
export const defaultStyles = StyleSheet.create({
    textStyle: {
      color: "black",
      fontSize: 14,
      textAlign: "auto",
      fontStyle: "normal",
    },
  });